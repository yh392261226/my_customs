/* 选择书籍弹窗（SelectBooksDialog）专用样式覆盖
   注意：全部使用以 #select-books- 前缀的唯一 ID，避免影响其他页面
*/
#select-books-dialog {
    width: 90%;
    height: 85%;
    background: $panel;
    border: round $primary;
    padding: 1;
    layout: vertical;
}

/* 根容器 */
#select-books-root {
    padding: 1 2;
}

/* 标题 */
#select-books-title {
    color: $text;
    text-style: bold;
    margin: 0 0 1 0;
}

/* ID范围输入行 */
#id-range-row {
    layout: horizontal;
    height: 3;
    align: center middle;
}

/* 输入框宽度适配（仅弹窗内） */
#id-range-row Input {
    width: 20;
}

#seprator {
    padding: 1 0;
}

/* 结果表格：占据剩余空间 */
#select-books-root #books-table {
    height: 95%;
    text-wrap: wrap;
}

/* 按钮行 */
#buttons-row {
    layout: horizontal;
    height: 4;
}

/* 主按钮与取消按钮的配色（仅此弹窗作用域） */
#buttons-row Button#ok-btn {
    background: $success;
    color: $surface;
}

#buttons-row Button#cancel-btn {
    background: $error;
    color: $surface;
}

.book-select {
    width: 2;
    height: 2;
}

#books-stats-label {
    width: 100%;
    height: 5;
    overflow-y: auto;
    text-wrap: wrap;
    align: center middle;
    text-align: center;
}

/* 加载动画样式 */
#loading-animation {
    height: 3;
    align: center middle;
    text-align: center;
    color: $accent;
    text-style: bold;
    min-height: 3;
    background: $panel;
    border: round $primary;
    margin: 1 0;
}

.loading-animation-container {
    height: 3;
    min-height: 3;
    align: center middle;
    text-align: center;
}

/* 加载指示器样式 */
#select-books-loading-indicator {
    height: 3;
    align: center middle;
}

/* 加载覆盖层样式 */
#select-books-loading-overlay {
    align: center middle;
    text-align: center;
}