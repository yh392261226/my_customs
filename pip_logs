pip install -U yapf

python3 -m pip install -U pynvim neovim

pip install -U ale

pip install -U notedown

pip install -U glances

pip install -U dtags

pip install -U lesspass

pip install shell-functools

pip install virtualenv

pip install haxor-news

pip install isitup

pip3 install --user --upgrade advance-touch

pip install percol

pip install ippy

pip install tox

pip install openpyxl

pip3 install csvkit

pip3 install -U python-language-server

pip3 install -U tree_sitter

pip3 install -U easyocr

pip install -U pytesseract

pip install -U pillow

pip install -U httpcat

pip install -U http-prompt

pip install -U httpie-snapdsocket

pip install -U httpie-unixsocket

pip install -U httpie-ntlm

pip install -U httpie httpie-http2
