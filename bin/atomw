#!/usr/bin/env bash
###Desc: atom which
###Author: Json
COMMANDBIN=/usr/local/bin/atom
if [ -f $COMMANDBIN ]; then
    if [ "" != "$1" ]; then
    	$COMMANDBIN `which "$1"`
    else
    	$COMMANDBIN `pwd`
    fi
fi
