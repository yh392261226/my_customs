#!/usr/bin/env bash
###Desc: code which
###Author: Json
COMMANDBIN=/usr/local/bin/code
if [ -f $COMMANDBIN ]; then
    if [ "" != "$1" ]; then
    	$COMMANDBIN `which "$1"`
    else
    	$COMMANDBIN `pwd`
    fi
fi
