#!/usr/bin/env bash

if [ "" != "$1" ]; then
    echo "which $1 : "
    /usr/bin/which $1
    echo "whereis $1 : "
    /usr/bin/whereis $1
    echo "type $1 : "
    /usr/bin/type $1
    echo "command $1 :"
    /usr/bin/command $1
else
    echo "Usage:$0 command"
fi
